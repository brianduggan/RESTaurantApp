<div class='party-content'>
  <ul>
    <% @users.each do |user| %>
    <li>
      <%= user.username %>, <%=user.permission %>, <%= link_to "Edit", edit_user_path(user) %>
    </li>
    <% end %>
  </ul>
</div>
