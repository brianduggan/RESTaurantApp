<h1>Edit <%= @order.food.name %></h1>

<%= form_for @order, url: party_order_path(@order.party_id, @order) do |f|%>
  <%= f.label "Notes: "%><br>
  <%= f.text_area :notes %>
  <%= f.submit %>
<% end %>
