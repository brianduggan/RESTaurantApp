<h1>Your Menu!</h1>

<ul>
<% @foods.each do |food|%>
  <li>
  <%= food.name %>, <%= food.description %>, $<%= food.price %>, <%= link_to "Edit Menu Item", edit_food_path(food.id) %><%= button_to "Delete", food_path(food), method: :delete %>
  </li>
<% end %>
</ul>

<%= link_to "back to the admin page", user_path(current_user)  %>
